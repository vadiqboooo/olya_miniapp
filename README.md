# Фитнес приложение - Программы тренировок

Веб-приложение для создания и отслеживания программ тренировок с админ-панелью для управления контентом.

## 🚀 Технологии

- **Backend**: FastAPI, SQLAlchemy, SQLite
- **Frontend**: React 19, React Router, Vite
- **Deployment**: Docker, Docker Compose, Nginx

## 📁 Структура проекта

```
miniApp_olya/
├── backend/                 # FastAPI backend
│   ├── main.py             # Главный файл приложения
│   ├── models.py           # SQLAlchemy модели
│   ├── schemas.py          # Pydantic схемы
│   ├── crud.py             # CRUD операции
│   └── init_db.py          # Инициализация БД
├── fitness-client/         # React frontend
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   └── main.jsx        # Точка входа
│   └── public/             # Статические файлы
├── docker-compose.yml      # Docker оркестрация
├── ADMIN_PANEL_TZ.md       # Техническое задание
└── README.Docker.md        # Docker инструкции
```

## 🏃 Быстрый старт

### Вариант 1: Локальная разработка (без Docker)

#### Backend

```bash
# Перейти в директорию backend
cd backend

# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Инициализировать базу данных
python init_db.py

# Запустить сервер
uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

Backend будет доступен по адресу: http://127.0.0.1:8000
API документация: http://127.0.0.1:8000/docs

#### Frontend

```bash
# Перейти в директорию frontend (в новом терминале)
cd fitness-client

# Установить зависимости
npm install

# Запустить dev сервер
npm run dev
```

Frontend будет доступен по адресу: http://localhost:5173

---

### Вариант 2: Docker (рекомендуется для продакшена)

#### Требования

- Docker 20.10+
- Docker Compose 2.0+

#### Запуск

```bash
# 1. Настроить переменные окружения
cp .env.production .env
# Отредактировать .env и указать ваш домен

# 2. Собрать и запустить контейнеры
docker compose up -d --build

# 3. Инициализировать базу данных (первый запуск)
docker compose exec backend python init_db.py

# 4. Проверить статус
docker compose ps

# 5. Просмотр логов
docker compose logs -f
```

Приложение будет доступно:
- **Frontend**: http://localhost
- **Backend**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs

#### Остановка

```bash
docker compose down
```

Подробная документация по Docker: [README.Docker.md](README.Docker.md)

---

## 📚 API Endpoints

### Программы

- `GET /programs/` - Получить все программы (с фильтрами)
- `GET /programs/{id}` - Получить программу по ID
- `POST /programs/` - Создать новую программу
- `PUT /programs/{id}` - Обновить программу
- `DELETE /programs/{id}` - Удалить программу

### Тренировки (Дни)

- `GET /workouts/{program_id}` - Получить все дни программы
- `GET /workouts/{workout_id}/exercises` - Получить упражнения дня
- `POST /workouts/` - Создать день тренировки
- `PUT /workouts/{id}` - Обновить день
- `DELETE /workouts/{id}` - Удалить день

### Упражнения

- `POST /exercises/` - Создать упражнение
- `PUT /exercises/{id}` - Обновить упражнение
- `DELETE /exercises/{id}` - Удалить упражнение

### Прогресс пользователей

- `POST /progress/` - Создать запись прогресса
- `PATCH /progress/{id}/complete` - Отметить тренировку выполненной
- `GET /users/{id}/progress` - Получить прогресс пользователя

Полная документация: http://localhost:8000/docs

---

## 🛠️ Разработка

### Backend разработка

```bash
cd backend

# Запуск с hot-reload
uvicorn main:app --reload

# Запуск тестов (если добавлены)
pytest

# Проверка типов
mypy .
```

### Frontend разработка

```bash
cd fitness-client

# Запуск dev сервера
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр production сборки
npm run preview

# Линтинг
npm run lint
```

### Работа с БД

```bash
# Пересоздать базу данных
cd backend
rm workout_app.db
python init_db.py

# В Docker
docker compose exec backend rm data/workout_app.db
docker compose exec backend python init_db.py
```

---

## 🔧 Конфигурация

### Backend (.env)

```env
DATABASE_URL=sqlite:///./workout_app.db
API_HOST=127.0.0.1
API_PORT=8000
DEBUG=True
```

### Frontend (.env)

```env
VITE_API_URL=http://127.0.0.1:8000
```

---

## 📱 Пользовательские роуты

- `/` - Онбординг (выбор параметров)
- `/tracker/:programId` - Трекер тренировок (список дней)
- `/tracker/:programId/workout/:workoutId` - Детали тренировки (упражнения)

## 🔐 Админ-панель (в разработке)

Планируемые роуты:
- `/admin/programs` - Список всех программ
- `/admin/programs/create` - Создание программы
- `/admin/programs/:id/edit` - Редактирование программы
- `/admin/programs/:id/days` - Управление днями
- `/admin/programs/:id/days/:dayId/exercises` - Управление упражнениями

Подробное ТЗ: [ADMIN_PANEL_TZ.md](ADMIN_PANEL_TZ.md)

---

## 🐛 Troubleshooting

### Проблема: CORS ошибки

**Решение**: Убедитесь, что backend разрешает запросы с вашего frontend URL в `main.py`:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],  # Добавьте ваш URL
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### Проблема: Frontend не подключается к Backend

**Решение**: Проверьте `VITE_API_URL` в `.env` файле и убедитесь, что backend запущен.

### Проблема: База данных не создается

**Решение**: Запустите `init_db.py` вручную:

```bash
cd backend
python init_db.py
```

---

## 📝 Лицензия

Проект разработан для учебных целей.

---

## 👥 Контакты

Для вопросов и предложений создайте issue в репозитории.

---

**Дата обновления**: 27.01.2026
**Версия**: 1.0
