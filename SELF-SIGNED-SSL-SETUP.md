# Настройка HTTPS с самоподписанным SSL для IP адреса

## 📋 Что это?

Это решение позволяет использовать HTTPS напрямую по IP адресу (`https://64.188.73.81`) без домена.

**⚠️ Важно:** Браузеры будут показывать предупреждение "Ваше подключение не защищено" - это нормально для самоподписанных сертификатов. Просто нажмите "Дополнительно" → "Продолжить на сайт".

## 🚀 Быстрый старт

### На сервере выполните:

```bash
# 1. Подключитесь к серверу
ssh root@64.188.73.81

# 2. Перейдите в папку проекта
cd /path/to/your/project
git pull origin main

# 3. Остановите старые контейнеры
docker-compose down

# 4. Сгенерируйте самоподписанный SSL сертификат
chmod +x generate-self-signed-ssl.sh
./generate-self-signed-ssl.sh

# 5. Запустите приложение с HTTPS
docker-compose -f docker-compose.self-signed-ssl.yml up -d --build

# 6. Проверьте статус
docker-compose -f docker-compose.self-signed-ssl.yml ps
```

## ✅ Готово!

Приложение доступно по адресу: **https://64.188.73.81**

## 🔓 Как обойти предупреждение браузера

### Chrome / Edge:
1. Увидите "Ваше подключение не защищено"
2. Нажмите **"Дополнительно"** или **"Advanced"**
3. Нажмите **"Продолжить на сайт 64.188.73.81 (небезопасно)"** или **"Proceed to 64.188.73.81 (unsafe)"**

### Firefox:
1. Увидите "Предупреждение: потенциальная угроза безопасности"
2. Нажмите **"Дополнительно..."**
3. Нажмите **"Принять риск и продолжить"**

### Safari:
1. Увидите "Это подключение не является приватным"
2. Нажмите **"Показать подробности"**
3. Нажмите **"Посетить этот сайт"**

### Mobile (Android/iOS):
1. Нажмите "Дополнительно" или "Advanced"
2. Нажмите "Продолжить на сайт"

## 🔍 Проверка работы

### 1. Проверьте HTTPS доступ
```bash
curl -k https://64.188.73.81/health
# Должен вернуть: healthy
```

### 2. Проверьте API
```bash
curl -k https://64.188.73.81/api/programs/
```

### 3. Откройте в браузере
Откройте: **https://64.188.73.81**

Пропустите предупреждение безопасности и убедитесь, что приложение работает.

## 🔄 Архитектура

```
Пользователь → HTTPS (443) → Nginx (с самоподписанным SSL) →
                                    ├─> Frontend (Static files)
                                    └─> /api → Backend (8000)
```

## 📝 Управление

### Просмотр логов
```bash
docker-compose -f docker-compose.self-signed-ssl.yml logs -f
docker-compose -f docker-compose.self-signed-ssl.yml logs frontend
docker-compose -f docker-compose.self-signed-ssl.yml logs backend
```

### Перезапуск
```bash
docker-compose -f docker-compose.self-signed-ssl.yml restart
```

### Остановка
```bash
docker-compose -f docker-compose.self-signed-ssl.yml down
```

### Полная пересборка
```bash
docker-compose -f docker-compose.self-signed-ssl.yml down
docker-compose -f docker-compose.self-signed-ssl.yml up -d --build
```

## 🔄 Обновление сертификата

Самоподписанный сертификат действует 365 дней. Для обновления:

```bash
# Удалите старый
rm -rf ./ssl-self-signed

# Создайте новый
./generate-self-signed-ssl.sh

# Перезапустите контейнеры
docker-compose -f docker-compose.self-signed-ssl.yml restart frontend
```

## ❓ Устранение проблем

### Проблема: "NET::ERR_CERT_AUTHORITY_INVALID"

**Это нормально!** Это предупреждение появляется потому, что сертификат самоподписанный.

Решение: Нажмите "Дополнительно" → "Продолжить на сайт"

### Проблема: Не открывается страница в мобильном браузере

Некоторые мобильные браузеры строго относятся к самоподписанным сертификатам.

Решения:
1. Используйте Chrome или Firefox на мобильном
2. Или настройте доменное имя с Let's Encrypt (см. SSL-SETUP.md)

### Проблема: API запросы не работают

1. Проверьте логи backend:
```bash
docker-compose -f docker-compose.self-signed-ssl.yml logs backend
```

2. Проверьте прямой доступ:
```bash
curl http://localhost:8000/programs/
```

3. Проверьте nginx:
```bash
docker-compose -f docker-compose.self-signed-ssl.yml logs frontend
```

### Проблема: Порт 443 уже занят

```bash
# Найдите процесс, использующий порт 443
sudo lsof -i :443

# Остановите конфликтующий сервис
sudo systemctl stop apache2  # или другой веб-сервер
```

## 🔒 Безопасность

**Ограничения самоподписанных сертификатов:**
- ❌ Браузеры показывают предупреждения
- ❌ Нет автоматической проверки подлинности
- ❌ Не подходит для публичных сайтов
- ✅ Шифрует трафик между клиентом и сервером
- ✅ Подходит для разработки и внутренних приложений

**Для production рекомендуется:**
- Настроить доменное имя
- Использовать Let's Encrypt (бесплатный, доверенный)
- См. инструкцию в SSL-SETUP.md

## 🎯 Альтернативы

### Вариант 1: Бесплатный домен
Зарегистрируйте бесплатный поддомен на:
- Freenom (бесплатные домены .tk, .ml, .ga)
- DuckDNS (бесплатные поддомены)
- NoIP (бесплатные поддомены)

Затем используйте Let's Encrypt по инструкции из SSL-SETUP.md

### Вариант 2: Работа без HTTPS
Используйте HTTP (порт 80) по инструкции из QUICK-START-NO-SSL.md

### Вариант 3: Исправить текущий домен
Исправьте DNS для vm725864.hosted-by.u1host.com (см. SSL-SETUP.md)

## 📊 Полезные команды

```bash
# Проверить сертификат
openssl x509 -in ./ssl-self-signed/fullchain.pem -text -noout

# Проверить срок действия
openssl x509 -in ./ssl-self-signed/fullchain.pem -noout -dates

# Тест SSL соединения
openssl s_client -connect 64.188.73.81:443

# Проверка с curl (игнорируя проверку сертификата)
curl -k https://64.188.73.81/

# Статус контейнеров
docker-compose -f docker-compose.self-signed-ssl.yml ps

# Использование дискового пространства
docker system df
```

## 💡 Советы

1. **Добавьте сертификат в доверенные** (опционально):
   - На ПК: импортируйте `fullchain.pem` в хранилище сертификатов ОС
   - Это уберет предупреждение браузера на вашем компьютере

2. **Для мобильных устройств**:
   - Отправьте себе сертификат по email
   - Установите его на устройстве
   - Так можно убрать предупреждение на мобильном

3. **Сохраните сертификат**:
   - Папка `ssl-self-signed/` добавлена в `.gitignore`
   - Сделайте backup сертификатов отдельно

## 🔗 Связанные документы

- `SSL-SETUP.md` - полная настройка HTTPS с доменом и Let's Encrypt
- `QUICK-START-NO-SSL.md` - запуск без SSL (HTTP)
- `README.Docker.md` - общая документация по Docker

---

**Вопросы?** Проверьте логи: `docker-compose -f docker-compose.self-signed-ssl.yml logs -f`
