# SSL настройка для miniapp.rancheasy.ru

## 🎯 Быстрая справка

**Домен:** miniapp.rancheasy.ru
**IP:** 64.188.73.81
**SSL:** Let's Encrypt (бесплатный, доверенный)
**Срок действия:** 90 дней (автоматическое обновление)

---

## 📋 Краткая инструкция

### 1. Проверьте DNS
```bash
nslookup miniapp.rancheasy.ru 8.8.8.8
# Должен вернуть: 64.188.73.81
```

### 2. Получите SSL сертификат
```bash
# Отредактируйте email в скрипте
nano setup-ssl.sh

# Запустите
chmod +x setup-ssl.sh
./setup-ssl.sh
```

### 3. Запустите приложение
```bash
docker-compose up -d --build
```

### 4. Готово!
Откройте: **https://miniapp.rancheasy.ru**

---

## 📚 Полная документация

См. файл **DEPLOY.md** для подробной инструкции.

---

## 🔄 Обновление сертификата

Автоматически каждые 12 часов через контейнер certbot.

Ручное обновление:
```bash
docker-compose run --rm certbot renew
docker-compose restart frontend
```

---

## ❓ Проблемы?

### DNS не обновился
Подождите 5-60 минут после изменения DNS записей.

### Не удается получить сертификат
```bash
# Проверьте порт 80
sudo lsof -i :80

# Остановите конфликтующие сервисы
sudo systemctl stop apache2
```

### Приложение не работает
```bash
# Проверьте логи
docker-compose logs -f

# Пересоберите
docker-compose down
docker-compose up -d --build
```

---

**Подробнее:** См. DEPLOY.md
