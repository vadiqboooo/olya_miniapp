# Быстрый запуск БЕЗ SSL (временное решение)

## 🚨 Проблема с DNS

Домен `vm725864.hosted-by.u1host.com` настроен неправильно и указывает на localhost.
Это временное решение для запуска приложения без HTTPS, пока DNS не исправлен.

## 📋 Быстрый старт на сервере

### 1. Подключитесь к серверу
```bash
ssh root@64.188.73.81
```

### 2. Перейдите в папку проекта
```bash
cd /path/to/your/project
git pull origin main
```

### 3. Остановите старые контейнеры
```bash
docker-compose down
```

### 4. Запустите с временной конфигурацией БЕЗ SSL
```bash
docker-compose -f docker-compose.nossl.yml up -d --build
```

### 5. Проверьте статус
```bash
docker-compose -f docker-compose.nossl.yml ps
docker-compose -f docker-compose.nossl.yml logs -f
```

## ✅ Готово!

Приложение доступно по адресу: **http://64.188.73.81** (без HTTPS)

## 🔧 Как исправить DNS и включить HTTPS

### Шаг 1: Исправьте DNS записи

Зайдите в панель управления хостингом **u1host.com** и настройте DNS:

**Удалите:**
- Любые CNAME записи на localhost

**Создайте/Измените A-запись:**
```
Тип: A
Имя: @ или vm725864.hosted-by.u1host.com
Значение: 64.188.73.81
TTL: 3600
```

### Шаг 2: Дождитесь распространения DNS (5-60 минут)

Проверьте DNS:
```bash
# Должен вернуть 64.188.73.81, а не 127.0.0.1
nslookup vm725864.hosted-by.u1host.com 8.8.8.8
```

Или онлайн: https://dnschecker.org/#A/vm725864.hosted-by.u1host.com

### Шаг 3: После исправления DNS переключитесь на HTTPS

```bash
# Остановите временную конфигурацию
docker-compose -f docker-compose.nossl.yml down

# Получите SSL сертификат
nano get-ssl.sh  # Укажите свой email
chmod +x get-ssl.sh
./get-ssl.sh

# Запустите с SSL
docker-compose up -d --build
```

## 📊 Проверка работы

```bash
# Проверить backend
curl http://64.188.73.81:8000/programs/

# Проверить frontend
curl -I http://64.188.73.81/

# Посмотреть логи
docker-compose -f docker-compose.nossl.yml logs backend
docker-compose -f docker-compose.nossl.yml logs frontend
```

## 🆘 Если нужна помощь с DNS

Обратитесь в поддержку **u1host.com** с таким запросом:

```
Здравствуйте!

Прошу настроить DNS запись для моего домена:

Домен: vm725864.hosted-by.u1host.com
Тип записи: A
Значение: 64.188.73.81

Текущая проблема: домен указывает на localhost (127.0.0.1)
вместо моего сервера.

Спасибо!
```

## 📝 Полезные команды

```bash
# Перезапустить все контейнеры
docker-compose -f docker-compose.nossl.yml restart

# Остановить все
docker-compose -f docker-compose.nossl.yml down

# Посмотреть логи в реальном времени
docker-compose -f docker-compose.nossl.yml logs -f

# Проверить статус
docker-compose -f docker-compose.nossl.yml ps
```

## ⚠️ Важно

Это временное решение! После исправления DNS обязательно переключитесь на HTTPS для безопасности.
