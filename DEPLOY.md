# 🚀 Развертывание приложения на miniapp.rancheasy.ru

Полная инструкция по развертыванию фитнес-приложения с HTTPS (Let's Encrypt SSL).

## 📋 Предварительные требования

### 1. DNS настроен
Убедитесь, что домен указывает на ваш сервер:

```bash
nslookup miniapp.rancheasy.ru 8.8.8.8
# Должен вернуть: 64.188.73.81
```

Если DNS не настроен, добавьте **A-запись** в панели управления доменом:
```
Тип: A
Имя: miniapp
Значение: 64.188.73.81
TTL: 3600
```

### 2. Порты открыты
```bash
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 8000/tcp
sudo ufw status
```

### 3. Docker установлен
```bash
docker --version
docker-compose --version
```

---

## 🔧 Шаг 1: Подготовка сервера

```bash
# Подключитесь к серверу
ssh root@64.188.73.81

# Перейдите в папку проекта или склонируйте репозиторий
cd /opt
git clone https://github.com/vadiqboooo/olya_miniapp.git
cd olya_miniapp

# Или обновите существующий:
cd /path/to/your/project
git pull origin main
```

---

## 🔒 Шаг 2: Получение SSL сертификата

### Отредактируйте скрипт (укажите свой email):
```bash
nano setup-ssl.sh
```

Измените строку:
```bash
EMAIL="your-email@example.com"
# на ваш реальный email, например:
EMAIL="admin@rancheasy.ru"
```

Сохраните: **Ctrl + O**, **Enter**, выйдите: **Ctrl + X**

### Запустите скрипт:
```bash
chmod +x setup-ssl.sh
./setup-ssl.sh
```

Скрипт:
1. ✅ Проверит DNS
2. ✅ Создаст директории для сертификатов
3. ✅ Получит SSL сертификат от Let's Encrypt
4. ✅ Покажет информацию о сертификате

**Важно:** Если DNS еще не обновился, подождите 5-10 минут и запустите снова.

---

## 🚀 Шаг 3: Запуск приложения

```bash
# Запустите с HTTPS
docker-compose up -d --build

# Проверьте статус
docker-compose ps
```

Вы должны увидеть 3 контейнера:
- ✅ fitness-backend (работает)
- ✅ fitness-frontend (работает)
- ✅ fitness-certbot (работает)

---

## ✅ Готово!

Приложение доступно по адресу: **https://miniapp.rancheasy.ru**

🔒 Замочек в браузере будет **зеленым** (доверенный сертификат Let's Encrypt)

---

## 📊 Проверка работы

### 1. Проверьте HTTPS
```bash
curl -I https://miniapp.rancheasy.ru
# Должен вернуть: HTTP/2 200
```

### 2. Проверьте API
```bash
curl https://miniapp.rancheasy.ru/api/programs/
# Должен вернуть JSON с программами
```

### 3. Проверьте сертификат
```bash
openssl s_client -connect miniapp.rancheasy.ru:443 -servername miniapp.rancheasy.ru | grep "Verify return code"
# Должен вернуть: Verify return code: 0 (ok)
```

### 4. Откройте в браузере
Перейдите: **https://miniapp.rancheasy.ru**

Должны увидеть:
- ✅ Зеленый замочек
- ✅ Приложение загружается
- ✅ API работает

---

## 🔄 Автоматическое обновление сертификата

Сертификат Let's Encrypt действует 90 дней.

Контейнер **certbot** автоматически проверяет и обновляет сертификат каждые 12 часов.

### Ручное обновление:
```bash
docker-compose run --rm certbot renew
docker-compose restart frontend
```

### Проверка срока действия:
```bash
docker-compose run --rm certbot certificates
```

---

## 📝 Управление приложением

### Просмотр логов
```bash
# Все логи
docker-compose logs -f

# Только frontend
docker-compose logs -f frontend

# Только backend
docker-compose logs -f backend
```

### Перезапуск
```bash
# Перезапустить все
docker-compose restart

# Перезапустить конкретный сервис
docker-compose restart frontend
docker-compose restart backend
```

### Остановка
```bash
docker-compose down
```

### Полная пересборка
```bash
docker-compose down
docker-compose up -d --build --force-recreate
```

### Обновление кода
```bash
git pull origin main
docker-compose down
docker-compose up -d --build
```

---

## 🔧 Устранение проблем

### Проблема: DNS не обновился

**Симптомы:** `nslookup` возвращает неправильный IP

**Решение:**
1. Проверьте настройки DNS в панели управления доменом
2. Подождите 5-60 минут (время распространения DNS)
3. Очистите DNS кэш: `sudo systemd-resolve --flush-caches`

### Проблема: Не удается получить сертификат

**Симптомы:** `setup-ssl.sh` завершается с ошибкой

**Решение:**
```bash
# Проверьте, что порт 80 свободен
sudo lsof -i :80

# Если порт занят, остановите сервис
sudo systemctl stop apache2
sudo systemctl stop nginx

# Проверьте DNS
nslookup miniapp.rancheasy.ru 8.8.8.8

# Попробуйте снова
./setup-ssl.sh
```

### Проблема: Приложение не запускается

**Решение:**
```bash
# Проверьте логи
docker-compose logs

# Проверьте, что порты не заняты
sudo lsof -i :80
sudo lsof -i :443
sudo lsof -i :8000

# Пересоберите с нуля
docker-compose down -v
docker-compose up -d --build
```

### Проблема: API не работает

**Решение:**
```bash
# Проверьте backend
docker-compose logs backend

# Проверьте прямой доступ
curl http://localhost:8000/programs/

# Перезапустите backend
docker-compose restart backend
```

### Проблема: Mixed Content (HTTP вместо HTTPS)

**Решение:**
```bash
# Пересоберите frontend БЕЗ кэша
docker-compose down
docker rmi fitness-frontend
docker-compose up -d --build

# Очистите кэш браузера: Ctrl + Shift + R
```

---

## 🔐 Безопасность

1. ✅ HTTPS с доверенным сертификатом Let's Encrypt
2. ✅ Автоматическое обновление сертификата
3. ✅ Security headers (HSTS, X-Frame-Options, etc.)
4. ✅ API проксируется через Nginx (backend недоступен напрямую)
5. ✅ Gzip сжатие для быстрой загрузки

---

## 📚 Полезные ссылки

- **Приложение:** https://miniapp.rancheasy.ru
- **API:** https://miniapp.rancheasy.ru/api/programs/
- **Репозиторий:** https://github.com/vadiqboooo/olya_miniapp
- **Let's Encrypt:** https://letsencrypt.org/

---

## 💡 Советы

1. **Мониторинг:** Настройте мониторинг uptime (UptimeRobot, Pingdom)
2. **Backup:** Регулярно делайте backup базы данных (`./backend/data/`)
3. **Логи:** Настройте ротацию логов Docker
4. **Обновления:** Регулярно обновляйте Docker образы

```bash
# Обновление образов
docker-compose pull
docker-compose up -d
```

---

**Вопросы?** Проверьте логи: `docker-compose logs -f`
